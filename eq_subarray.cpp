#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,k;
    cin>>n>>k;
    int a[n];
    
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    
    for(int len=n-1;len>=0;len--)
    {
        start=0;
        end=len;
        int max=*max_ele
        for(int j=start;j<len;j++)
        {
            int max=*max_element(a+start,a+len);
            
        }
    }
}